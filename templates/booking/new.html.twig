{% extends 'base.html.twig' %}

{% block title %}New Booking{% endblock %}

{% block body %}
    <h1>Réservation de la place {{ places.title }}</h1>

    {{ include('booking/_form.html.twig') }}

    <a href="{{ path('home') }}">back to list</a>
{% endblock %}
