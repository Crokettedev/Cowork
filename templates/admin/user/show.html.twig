{% extends 'base2.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Information de l'administrateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_user_index') }}" class="btn btn-primary">Revenir en arrière</a>

    <a href="{{ path('admin_user_edit', {'id': user.id}) }}" class="btn btn-secondary">Modifier</a> <br><br>

    {{ include('admin/user/_delete_form.html.twig') }}
{% endblock %}
