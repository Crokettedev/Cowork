{% extends 'base2.html.twig' %}

{% block title %}Espace publique{% endblock %}

{% block body %}
    <h1>Information</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ place_public.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ place_public.title }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ place_public.price }} €</td>
            </tr>


        </tbody>
    </table>

    <a href="{{ path('admin_place_public_index') }}" class="btn btn-primary">Revenir en arrière</a>

    <a href="{{ path('admin_place_public_edit', {'id': place_public.id}) }}" class="btn btn-secondary">Editer</a><br><br>

    {{ include('admin/place_public/_delete_form.html.twig') }}
{% endblock %}
