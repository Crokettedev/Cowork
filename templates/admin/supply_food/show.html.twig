{% extends 'base2.html.twig' %}

{% block title %}SupplyFood{% endblock %}

{% block body %}
    <h1>SupplyFood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ supply_food.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ supply_food.filename }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ supply_food.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ supply_food.content }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ supply_food.price }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ supply_food.stock }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ supply_food.label }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ supply_food.createdAt ? supply_food.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ supply_food.updatedAt ? supply_food.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_supply_food_index') }}">back to list</a>

    <a href="{{ path('admin_supply_food_edit', {'id': supply_food.id}) }}">edit</a>

    {{ include('admin/supply_food/_delete_form.html.twig') }}
{% endblock %}
